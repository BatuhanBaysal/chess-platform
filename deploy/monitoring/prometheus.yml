global:
    scrape_interval: 15s

scrape_configs:
    - job_name: 'chess-backend-docker'
      dockerswarm_sd_configs: ~
      metrics_path: '/actuator/prometheus'
      dns_sd_configs:
          - names: ['backend']
            type: 'A'
            port: 8080
